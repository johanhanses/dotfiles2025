# Maximum items to keep per feed
max-items 500

# browser
# browser "/usr/lib/firefox/firefox %u"

# Linux/Ubuntu
# browser "xdg-open %u"

# macOS
# browser "open -g -a 'Firefox Nightly' %u"

# Browser configuration - Updated to use Google Chrome

# Linux/Ubuntu
# browser "google-chrome %u"

# macOS
# browser "open -g -a 'Google Chrome' %u"
# browser "open -g -a 'Safari' %u"

# Alternative macOS syntax:
browser "open -a 'Google Chrome' %u"

# Basic display settings
show-read-feeds yes
feed-sort-order unreadarticlecount-asc

# Colors and formatting
color background default default
color listnormal default default
color listfocus black yellow
color listnormal_unread default default bold
color listfocus_unread black yellow bold
color info black blue bold
color article default default

# Key bindings
bind-key j down
bind-key k up
bind-key J next-feed
bind-key K prev-feed
bind-key G end
bind-key g home
bind-key l open
bind-key h quit
